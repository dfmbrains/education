import React, {useEffect} from 'react';
import axios from "axios";

const MyCourses = () => {
    useEffect(() => {
       axios('')
    });
    return (
        <div>
            My Courses
        </div>
    );
};

export default MyCourses;